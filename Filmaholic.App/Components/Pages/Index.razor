@page "/"
@inject NavigationManager Nav
@inject AuthService AuthService

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo(AuthService.IsAuthenticated ? "/movies" : "/login", true);
    }
}
