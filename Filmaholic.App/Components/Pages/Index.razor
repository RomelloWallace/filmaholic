@page "/"
@inject NavigationManager Nav
@inject AuthService AuthService

@code {
    protected override async Task OnInitializedAsync()
    {
        Nav.NavigateTo(await AuthService.IsAuthenticatedAsync() ? "/movies" : "/login", true);
    }
}
