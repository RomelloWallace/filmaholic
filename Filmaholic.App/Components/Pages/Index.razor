@page "/"
@inject NavigationManager Nav
@inject AuthService AuthService

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo(AuthService.isAuthenticatedAsync ? "/movies" : "/login", true);
    }
}
